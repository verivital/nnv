classdef FlattenLayer
    % Stub class to satisfy class references in NNV when ONNX support is not installed

    properties
        Name
    end

    methods
        function obj = FlattenLayer()
            obj.Name = '';
        end
    end
end
